<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="praeterii.pixele" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Pixelesque" >
        <activity
            android:name="praeterii.pixels.PixelArtEditor"
            android:configChanges="orientation"
            android:hardwareAccelerated="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pixelesque" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="praeterii.pixels.artlist.ArtListActivity"
            android:configChanges="orientation" />
        <activity
            android:name="de.devmil.common.ui.color.ColorSelectorActivity"
            android:configChanges="orientation"
            android:theme="@style/myBackgroundStyle" />
    </application>

</manifest>